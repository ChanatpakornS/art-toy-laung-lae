NEXT_PUBLIC_BACKEND_API_URL=http://localhost:8000

# NextAuth expects `NEXTAUTH_URL` and `NEXTAUTH_SECRET` (no underscore).
# Create a local `.env.local` with these values for development.
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=TESTMENTR